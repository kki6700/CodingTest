#include <string>
#include <vector>

using namespace std;

double solution(vector<int> arr) {
    double answer = 0;
    int len = arr.size();
    double z = 0;
    int i = 0;
    while (i < len){
        z += arr[i];   
        i++;
        
    }
    answer = z/len;
    return answer;
}
